buildPack: apps
pipelineConfig:
  pipelines:
    release:
      pipeline:
        stages:
        - name: release
          environment:
            - name: GORELEASER_VERSION
              value: 0.93.2
          preSteps:
            command: mkdir
            args:
            - goreleaser


#                export GORELEASER_VERSION=0.93.2 && mkdir goreleaser && \
#                curl -Lf https://github.com/goreleaser/goreleaser/releases/download/v${GORELEASER_VERSION}/goreleaser_Linux_x86_64.tar.gz | tar -xzv -C ./goreleaser/ && \
#                mv goreleaser/goreleaser /usr/bin/ && \
#                rm -rf goreleaser